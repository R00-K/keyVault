import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const background = Color(0xFF0D1117);
  static const surface = Color(0xFF161B22);
  static const surfaceVariant = Color(0xFF21262D);

  static const primary = Color.fromARGB(255, 85, 51, 239);
  static const primaryMuted = Color(0xFF1F3A66);
  static const onPrimary = Color(0xFFFFFFFF);

  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);

  static const textPrimary = Color(0xFFFFFFFF);
  static const textMuted = Color(0xFFC9D1D9);
  static const textSecondary = Color(0xFF9CA3AF);

  static const divider = Color(0xFF2D333B);
}
